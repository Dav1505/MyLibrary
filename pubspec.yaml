name: mylibrary
description: "Progetto per Programmazione mobile"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk:  ^3.9.2

dependencies:
  flutter_localizations:
    sdk: flutter
  firebase_core: ^4.3.0
  cloud_firestore: ^6.1.1
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  cached_network_image: ^3.3.1

  flutter:
    sdk: flutter
  firebase_auth: ^6.1.3
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  dynamic_color: ^1.8.1
  flutter_dotenv: ^6.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - localizable/
